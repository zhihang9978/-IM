<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 网络权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    
    <!-- 存储权限 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    
    <!-- 相机和麦克风权限（音视频通话） -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    
    <!-- 通知权限 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    
    <!-- 前台服务权限 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:name=".LanxinApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LanxinIM"
        android:usesCleartextTraffic="true">
        
        <!-- 登录Activity（启动页） -->
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- 主Activity -->
        <activity
            android:name=".MainActivity"
            android:windowSoftInputMode="adjustResize" />

        
        <!-- 聊天Activity -->
        <activity
            android:name=".ui.chat.ChatActivity"
            android:windowSoftInputMode="adjustResize"
            android:parentActivityName=".MainActivity" />
        
        <!-- 设置Activity -->
        <activity
            android:name=".ui.settings.SettingsActivity"
            android:parentActivityName=".MainActivity" />
        
        <!-- 关于Activity -->
        <activity
            android:name=".ui.settings.AboutActivity"
            android:parentActivityName=".ui.settings.SettingsActivity" />
        
        <!-- 添加好友Activity -->
        <activity
            android:name=".ui.social.AddFriendActivity"
            android:parentActivityName=".MainActivity" />
        
        <!-- 投诉举报Activity -->
        <activity
            android:name=".ui.social.ReportActivity"
            android:parentActivityName=".MainActivity" />
        
        <!-- 扫一扫Activity -->
        <activity
            android:name=".ui.social.ScanQRCodeActivity"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen" />
        
        <!-- 收藏Activity -->
        <activity
            android:name=".ui.social.FavoritesActivity"
            android:parentActivityName=".MainActivity" />
        
        <!-- 搜索Activity -->
        <activity
            android:name=".ui.search.SearchActivity"
            android:windowSoftInputMode="stateAlwaysVisible"
            android:parentActivityName=".MainActivity" />
        
        <!-- 新的朋友Activity -->
        <activity
            android:name=".ui.contacts.NewFriendsActivity"
            android:parentActivityName=".MainActivity" />
        
        <!-- 群聊列表Activity -->
        <activity
            android:name=".ui.group.GroupListActivity"
            android:parentActivityName=".MainActivity" />
        
        <!-- 图片预览Activity -->
        <activity
            android:name=".ui.media.ImagePreviewActivity"
            android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen"
            android:parentActivityName=".ui.chat.ChatActivity" />
        
        <!-- 视频播放Activity -->
        <activity
            android:name=".ui.media.VideoPlayerActivity"
            android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen"
            android:parentActivityName=".ui.chat.ChatActivity" />
        
        <!-- 音视频通话Activity（使用腾讯云TRTC） -->
        <activity
            android:name=".trtc.AudioCallActivity"
            android:screenOrientation="portrait" />
        
        <activity
            android:name=".trtc.VideoCallActivity"
            android:screenOrientation="portrait" />
        
        <activity
            android:name=".trtc.ScreenShareActivity"
            android:screenOrientation="portrait" />
        
        <!-- FileProvider for camera and file operations -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>

