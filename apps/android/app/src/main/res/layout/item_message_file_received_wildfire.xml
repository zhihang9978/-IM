<?xml version="1.0" encoding="utf-8"?>
<!--
  Received file message item layout (WildFire IM style)
  Source: WildFireChat android-chat-master/uikit/res/layout/conversation_item_file_receive.xml (Apache 2.0)
  Adapted for LanXin IM
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_gravity="start"
    android:padding="8dp">

    <RelativeLayout
        android:id="@+id/fileMessageContentView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:minWidth="220dp"
        android:minHeight="80dp"
        android:background="@drawable/img_bubble_receive"
        android:backgroundTint="@color/white"
        android:padding="12dp">

        <!-- 文件图标 -->
        <ImageView
            android:id="@+id/fileIconImageView"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_centerVertical="true"
            android:src="@mipmap/ic_file_type_unknown"
            android:contentDescription="@string/file_message" />

        <!-- 文件名 -->
        <TextView
            android:id="@+id/fileNameTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_toEndOf="@id/fileIconImageView"
            android:layout_alignTop="@id/fileIconImageView"
            android:layout_marginStart="12dp"
            android:layout_marginEnd="8dp"
            android:ellipsize="end"
            android:maxWidth="180dp"
            android:maxLines="2"
            android:textColor="@color/textPrimary"
            android:textSize="15sp"
            tools:text="report_2023_annual.xlsx" />

        <!-- 文件大小 -->
        <TextView
            android:id="@+id/fileSizeTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_toEndOf="@id/fileIconImageView"
            android:layout_alignBottom="@id/fileIconImageView"
            android:layout_marginStart="12dp"
            android:textColor="@color/textSecondary"
            android:textSize="12sp"
            tools:text="520 KB" />

        <!-- 下载进度条（可选） -->
        <ProgressBar
            android:id="@+id/downloadProgressBar"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="match_parent"
            android:layout_height="4dp"
            android:layout_below="@id/fileIconImageView"
            android:layout_marginTop="8dp"
            android:visibility="gone"
            tools:visibility="visible"
            tools:progress="30" />

    </RelativeLayout>

</FrameLayout>

